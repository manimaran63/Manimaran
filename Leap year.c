#include<stdio.h>
#include<conio.h>
void main()
{
   int year;
   clrscr();
   printf("Enter the year");
   scanf("%d",&year);
   if(a%4==0)
   {
      printf("It is leap year");
   }
   else
   {
        printf("It is not leap year");
   }
   getch();
 }
