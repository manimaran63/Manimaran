#include<stdio.h>
#include<conio.h>
void main()
{
 int a,b,i,flag;
 clrscr();
 printf("Enter the two intervals:");
 scanf("%d%d",&a,&b);
 while(a<b)
 {
    flag=0;
    if(i=2;i<=a/2;++1)
    {
      if(a%i==0)
      {
       flag=1;
       break;
    }
 }
 if(flag==0)
 {
   printf("%d",a);
    ++a;
 } 
}
getch();
}
