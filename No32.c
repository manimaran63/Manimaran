#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
  char a[50];
  int i,count=0;
  clrscr();
  printf("Enter the string\n");
  scanf("%[^\n]s",&a[i]);
  for(i=0;s[i]!='\n';i++)
  {
     if(a[i]==' ')
     count++;
  }
  printf("number of words in the line is %d",count+1);
  getch();
}  
